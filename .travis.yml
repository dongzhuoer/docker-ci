dist: bionic
language: minimal
notifications:
  email: false

jobs:  
  include:
    - stage: build
      name: base
      script: bash +x build.sh base  
    - stage: test
      name: base
      script: bash +x test-base.sh
